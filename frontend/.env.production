# Production build env (Vite loads this only for `vite build` / mode=production).
# VITE_API_URL is an ORIGIN ONLY — no trailing slash and NO `/api` suffix;
# the code appends `/api` itself (see src/lib/api.ts and src/lib/auth.ts).
VITE_API_URL=https://archivedrop-api.wesam-w-abadi.workers.dev
